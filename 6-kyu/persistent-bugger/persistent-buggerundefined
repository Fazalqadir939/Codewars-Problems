int persistence(long long n){
  if (n <10)
  {
    return 0;
  }
  int counter = 0;
  int sum = 1;
  while(n !=0 )
  {
    sum *= n%10;
    n /= 10;
  }
  counter++;
  if(sum>9)
  return counter += persistence(sum);
  return counter;
}